---
phase: 05-voice-platform-stubs
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/voice/index.yaml
  - skills/system-prompt.md
autonomous: true
requirements: [PLAT-02, PLAT-03]

must_haves:
  truths:
    - "skills/voice/index.yaml registers the platform/ subfolder in its sub_folders array"
    - "system-prompt.md routing table includes a platform-aware routing row explaining the three-layer read order"
    - "system-prompt.md Voice section documents the platform/ directory and its files"
  artifacts:
    - path: "skills/voice/index.yaml"
      provides: "Voice index with platform subfolder registered"
      contains: "sub_folders"
    - path: "skills/system-prompt.md"
      provides: "Updated routing with platform context"
      contains: "platform"
  key_links:
    - from: "skills/system-prompt.md routing table"
      to: "skills/voice/platform/{platform}.md"
      via: "new routing row: Generate voice for a content type + platform"
      pattern: "platform"
---

<objective>
Update skills/voice/index.yaml to register the new platform/ subfolder, and update skills/system-prompt.md to document the platform files and add a platform-aware routing row to the routing table.

Purpose: Without index registration and routing guide update, the platform files are orphaned — Claude has no signal to load them. These updates close the loop and make the three-layer voice routing (category + platform + model) discoverable.

Output: 2 files updated — voice/index.yaml with sub_folders entry, system-prompt.md with platform documentation and routing row.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key interfaces and current file content the executor needs. -->

From skills/voice/index.yaml (current full content):
```yaml
folder: skills/voice
type: voice-index
files:
  - id: characteristics
    file: characteristics.md
    type: skill
    status: stub
    title: "Voice Characteristics"
    description: "General voice characteristics and tonal vocabulary"
  - id: hook-openers
    file: hook-openers.md
    type: voice-category
    status: stub
    title: "Hook Openers"
    description: "High-energy curiosity-driven openers — curious/excited delivery, non-uniform pacing"
  - id: cta-promo
    file: cta-promo.md
    type: voice-category
    status: stub
    title: "CTA / Promo"
    description: "Confident, warm, direct calls-to-action — urgency without desperation"
  - id: meme-quotes
    file: meme-quotes.md
    type: voice-category
    status: stub
    title: "Meme / Quotes"
    description: "Highly variable delivery — flat irony to exaggerated comedy, deadpan valid"
  - id: pov-relatable
    file: pov-relatable.md
    type: voice-category
    status: stub
    title: "POV / Relatable"
    description: "Conversational, intimate, first-person — warmth over authority"
  - id: instructional
    file: instructional.md
    type: voice-category
    status: stub
    title: "Instructional"
    description: "Clear, measured, warm authority — deliberate pacing, consistent register"
```

The index.schema.json allows an optional sub_folders array. Add it after the files array.

From skills/system-prompt.md (current routing table):
| User goal | Files to read |
|-----------|--------------|
| Generate music for a named genre | `genre/{super-genre}/{super-genre}.md` → `genre/{super-genre}/{genre}.md` → `minimax/music-2.5.md` |
| Generate music from a mood or vibe | `genre/taxonomy.md` → `genre/{super-genre}/{super-genre}.md` → `genre/{super-genre}/{genre}.md` → `minimax/music-2.5.md` |
| Specify structure or arrangement | `skills/music/structure-and-arrangement.md` → `minimax/music-2.5.md` |
| Specify tempo or key | `skills/music/tempo.md` or `keys-and-mood.md` → `minimax/music-2.5.md` |
| Add effects to instrumentation | `skills/music/effects.md` |
| Choose or describe instruments | `skills/music/instruments.md` |
| Generate voice for a content type | `voice/characteristics.md` → voice category file → model file |
| Choose between ElevenLabs models | `skills/tools/elevenlabs/eleven_multilingual_v2.md`, `eleven_turbo_v2_5.md`, `eleven_v3.md` |
| Generate voice with MiniMax speech | `voice/characteristics.md` → voice category file → `speech-02-hd.md` (reference) → generation-specific file |
| Add sound effects | `skills/sfx/taxonomy.md` |
| Create a new skill file | `skills/templates/` (reference schemas only) |

The current "Generate voice for a content type" row does not mention platform. Add a new row for platform-aware voice generation.

Current Voice section lists the 5 voice category files and characteristics.md. The platform/ subfolder needs to be documented here with its 3 files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update skills/voice/index.yaml — add sub_folders entry</name>
  <files>skills/voice/index.yaml</files>
  <action>
Read the current skills/voice/index.yaml (content shown in interfaces above). Append a sub_folders array after the closing of the files array. The index.schema.json allows an optional sub_folders array with entries requiring: id, folder, title (additionalProperties: false).

Add this block at the end of the file:

```yaml
sub_folders:
  - id: platform
    folder: skills/voice/platform
    title: "Platform Profiles"
```

The final file should have the full existing files array intact plus this sub_folders block appended. Do not modify any existing lines.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('/Users/jordymcintyre/audio-production-skills-documentation/skills/voice/index.yaml','utf8'); if(!c.includes('sub_folders'))throw new Error('missing sub_folders'); if(!c.includes('id: platform'))throw new Error('missing platform entry'); if(!c.includes('folder: skills/voice/platform'))throw new Error('missing platform folder path'); console.log('OK: voice/index.yaml updated');"</automated>
  </verify>
  <done>skills/voice/index.yaml has a sub_folders array with the platform entry (id: platform, folder: skills/voice/platform, title: "Platform Profiles"). All 6 existing files entries are preserved intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update system-prompt.md — add platform documentation and routing row</name>
  <files>skills/system-prompt.md</files>
  <action>
Make two targeted additions to skills/system-prompt.md:

**Addition 1 — Platform files subsection in the Voice section:**

After the existing "A voice characteristics file covers..." paragraph (the one ending with "always read it before writing voice settings guidance."), add a new paragraph block before the "Two-layer voice pattern:" heading:

```
**Platform profiles** — Three platform context files describe the prosodic norms specific to each short-form video platform. Read the platform file when the user specifies a target platform — it constrains the correct pacing, energy, and hook density for the generated voice.

| Platform | File |
|----------|------|
| TikTok          | `skills/voice/platform/tiktok.md` |
| YouTube Shorts  | `skills/voice/platform/youtube-shorts.md` |
| Instagram Reels | `skills/voice/platform/instagram-reels.md` |
```

**Addition 2 — New row in the Routing Guide table:**

Add a new row after the "Generate voice for a content type" row:

| Generate voice for a content type + platform | `voice/characteristics.md` → `voice/platform/{platform}.md` → voice category file → model file |

The "Two-layer voice pattern" heading and all content after it must remain exactly as-is. Do not rewrite any existing content — only insert these two targeted additions.

After both additions, read the file back to confirm the two insertions are present and no existing content was altered.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('/Users/jordymcintyre/audio-production-skills-documentation/skills/system-prompt.md','utf8'); if(!c.includes('Platform profiles'))throw new Error('missing Platform profiles section'); if(!c.includes('tiktok.md'))throw new Error('missing tiktok.md reference'); if(!c.includes('youtube-shorts.md'))throw new Error('missing youtube-shorts.md reference'); if(!c.includes('instagram-reels.md'))throw new Error('missing instagram-reels.md reference'); if(!c.includes('content type + platform'))throw new Error('missing routing row for platform'); if(!c.includes('Two-layer voice pattern'))throw new Error('Two-layer voice pattern section removed — must be preserved'); console.log('OK: system-prompt.md updated correctly');"</automated>
  </verify>
  <done>system-prompt.md has a Platform profiles table in the Voice section listing all 3 platform files, and a new routing row "Generate voice for a content type + platform" in the Routing Guide. All pre-existing content is preserved.</done>
</task>

</tasks>

<verification>
Confirm both files updated correctly:

1. `node scripts/validate-frontmatter.js` — no new errors introduced (system-prompt.md has type: skill, unchanged)
2. Grep confirms routing row exists: check system-prompt.md contains "content type + platform"
3. Grep confirms voice/index.yaml contains "sub_folders"
</verification>

<success_criteria>
- skills/voice/index.yaml has sub_folders array with platform entry pointing to skills/voice/platform
- system-prompt.md Voice section has a Platform profiles table with 3 file references
- system-prompt.md Routing Guide has a new row for "Generate voice for a content type + platform" with the three-layer read order
- All pre-existing content in both files is preserved
- validate-frontmatter.js reports no new errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-voice-platform-stubs/05-02-SUMMARY.md`
</output>
