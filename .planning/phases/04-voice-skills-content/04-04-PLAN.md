---
phase: 04-voice-skills-content
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/tools/minimax/speech-02-hd.md
  - skills/tools/minimax/speech-02-turbo.md
  - skills/tools/minimax/speech-2.6-hd.md
  - skills/tools/minimax/speech-2.6-turbo.md
  - skills/tools/minimax/speech-2.8-hd.md
  - skills/tools/minimax/speech-2.8-turbo.md
autonomous: true
requirements:
  - D3

must_haves:
  truths:
    - "All 6 MiniMax speech stub files are filled to draft status"
    - "speech-02-hd.md documents the full parameter set as the reference file"
    - "speech-02-turbo.md references speech-02-hd.md and notes HD vs Turbo trade-off"
    - "speech-2.6 files document added language support (Filipino, Tamil, Persian) over 02"
    - "speech-2.8 files document inline tone tag syntax alongside emotion param"
    - "All 6 files document: speed, vol, pitch, emotion parameters"
    - "All 6 files document MiniMax pause syntax: <#x#>"
    - "HD vs Turbo distinction is documented in each pair"
  artifacts:
    - path: "skills/tools/minimax/speech-02-hd.md"
      provides: "Reference MiniMax speech file — full parameter set"
    - path: "skills/tools/minimax/speech-2.8-hd.md"
      provides: "Latest MiniMax speech generation with tone tags"
  key_links:
    - from: "skills/tools/minimax/speech-02-hd.md"
      to: "skills/tools/minimax/speech-02-turbo.md"
      via: "Turbo references HD as full parameter source"
      pattern: "speech-02-hd"
---

<objective>
Fill all 6 MiniMax speech model stub files. speech-02-hd.md gets full content as the reference file. The remaining 5 files build on it, noting only per-generation differences.

Purpose: MiniMax is an available voice generation tool. These files give Claude the parameters needed to compose tool-ready speech prompts.
Output: 6 draft tool files covering all MiniMax speech model generations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-voice-skills-content/04-CONTEXT.md

<interfaces>
<!-- Locked MiniMax speech parameters from CONTEXT.md -->
Parameters (all models):
- speed: 0.5–2.0
- vol: normalised (no range specified — treat as 0.0–1.0 or note as normalised)
- pitch: −12 to +12 semitones. MiniMax only — no ElevenLabs equivalent.
- emotion: enum: happy, sad, angry, fearful, disgusted, surprised, calm. Model-wide, not per-phrase.
- pause: <#x#> syntax (e.g. <#0.5#> = 0.5s). Inline in text. All models.

Model generations (locked):
- speech-02-hd / speech-02-turbo: baseline stable. emotion param supported.
- speech-2.6-hd / speech-2.6-turbo: adds Filipino, Tamil, Persian. Otherwise identical to 02.
- speech-2.8-hd / speech-2.8-turbo: adds inline tone tags: (laughs), (chuckle), (sighs), (gasps), (clears throat), (coughs), (sneezes). emotion param still works alongside tags.

HD vs Turbo (all pairs): same params. HD = quality, Turbo = lower latency.

<!-- Current stub structure (read speech-02-hd.md before writing) -->
From skills/tools/minimax/speech-02-hd.md:
- Frontmatter: type: tool, status: stub, provider: MiniMax, model: Speech-02-HD, parameters: null, stable_styles: null, gotchas: null
- Sections: Purpose, Status, Provider, Model, Prompt Formula, Parameters, Stable Keywords, Gotchas, Output Format, Validated Patterns, Known Limitations

<!-- Tool schema: parameters must be object (not null) when status=draft -->
<!-- stable_styles can be null for speech models (no music style concept) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill speech-02-hd.md as the reference file</name>
  <files>skills/tools/minimax/speech-02-hd.md</files>
  <action>
    Rewrite speech-02-hd.md as a complete draft tool file. This is the reference — all other speech files point to it for the base parameter set.

    Frontmatter:
    ```yaml
    ---
    type: tool
    status: draft
    provider: MiniMax
    model: Speech-02-HD
    parameters:
      speed: "0.5–2.0. Default 1.0. Below 1 = slower, above 1 = faster."
      vol: "Normalised (0.0–1.0). Controls output volume level."
      pitch: "−12 to +12 semitones. Shifts voice register above/below baseline. MiniMax only — no ElevenLabs equivalent."
      emotion: "Enum: happy | sad | angry | fearful | disgusted | surprised | calm. Model-wide — applies to entire generation, not per-phrase."
    stable_styles: null
    gotchas:
      - "emotion is model-wide — cannot apply different emotions to different phrases in one generation"
      - "pitch shifts baseline but does not override the voice's fundamental character"
      - "vol is normalised — extreme values (0.0 or 1.0) may clip or be inaudible"
    ---
    ```

    Body sections:

    **## Purpose**
    MiniMax Speech-02-HD — the baseline high-quality TTS model. Use as the reference for all MiniMax speech parameters.

    **## Status: draft**

    **## Provider / ## Model** — standard fields

    **## Prompt Formula**
    MiniMax speech generation takes a text input and optional parameters. The text is the script. Parameters control delivery character.

    Compose your script first, then layer parameters:
    1. Write the script using natural spoken language
    2. Insert pause markers inline: `<#0.5#>` for 0.5s pause, `<#1#>` for 1s pause
    3. Set emotion if a global mood applies (optional)
    4. Adjust speed and pitch to match the target register and pacing

    **## Parameters**
    Document each parameter with range, default, and usage guidance in prose.

    **## Pause Syntax**
    `<#x#>` where x is seconds (decimals allowed). Examples: `<#0.3#>` = short beat, `<#1#>` = full pause. Insert inline in text: `I can't believe it. <#0.5#> That actually worked.`

    **## Gotchas**
    Expand the gotchas with practical notes.

    **## Output Format**
    Audio file generation. Submit text (with inline pause markers) + parameter values. No structured prompt format required — text is the prompt.

    **## Validated Patterns** / **## Known Limitations**
    *Populate through real usage testing*
  </action>
  <verify>
    Run: `node scripts/validate-frontmatter.js skills/tools/minimax/speech-02-hd.md` — PASS.
    Confirm: `grep "pitch\|emotion\|speed\|vol" skills/tools/minimax/speech-02-hd.md | grep -v "^#"` shows parameter entries.
    Confirm: `grep "<#" skills/tools/minimax/speech-02-hd.md` returns pause syntax examples.
  </verify>
  <done>speech-02-hd.md is status: draft, has all 4 parameters documented, includes pause syntax section, has gotchas array in frontmatter</done>
</task>

<task type="auto">
  <name>Task 2: Fill remaining 5 speech model files</name>
  <files>skills/tools/minimax/speech-02-turbo.md, skills/tools/minimax/speech-2.6-hd.md, skills/tools/minimax/speech-2.6-turbo.md, skills/tools/minimax/speech-2.8-hd.md, skills/tools/minimax/speech-2.8-turbo.md</files>
  <action>
    Fill all 5 remaining speech stub files. Each file notes its differences from the generation baseline. Reference speech-02-hd.md for the full parameter set; each file's body section tells Claude where to get the base content and then states only its differences.

    **speech-02-turbo.md**
    Frontmatter: same parameters as speech-02-hd. Gotchas: same + "lower latency than HD at slight quality cost — use Turbo for rapid iteration or real-time, use HD for final output". Status: draft, model: Speech-02-Turbo.
    Body: "Identical parameters and expression method to speech-02-hd.md. Differences: lower latency, slightly lower quality ceiling. Use HD for final production, Turbo for iteration or real-time applications."

    **speech-2.6-hd.md**
    Frontmatter: same parameters as speech-02-hd. Gotchas: same as speech-02-hd + "adds Filipino, Tamil, Persian language support — otherwise identical to speech-02 generation". Status: draft, model: Speech-2.6-HD.
    Body: "Same parameters and expression method as speech-02-hd.md. Generation 2.6 adds language support: Filipino, Tamil, Persian. No other differences."

    **speech-2.6-turbo.md**
    Frontmatter: same. Gotchas: combines 02-turbo and 2.6 notes. Model: Speech-2.6-Turbo.
    Body: "HD/Turbo trade-off same as 02 generation. Language additions same as speech-2.6-hd.md."

    **speech-2.8-hd.md**
    Frontmatter:
    ```yaml
    ---
    type: tool
    status: draft
    provider: MiniMax
    model: Speech-2.8-HD
    parameters:
      speed: "0.5–2.0. Default 1.0."
      vol: "Normalised (0.0–1.0)."
      pitch: "−12 to +12 semitones."
      emotion: "Enum: happy | sad | angry | fearful | disgusted | surprised | calm. Model-wide."
    stable_styles: null
    gotchas:
      - "emotion is model-wide — cannot apply different emotions per phrase"
      - "tone tags are inline in text (not model-wide) — unlike emotion param"
      - "tone tags and emotion can be used together — emotion sets the baseline, tags add phrase-level texture"
    ---
    ```
    Body: same base parameters as speech-02-hd.md. Document the **Tone Tags** section prominently:

    **## Tone Tags (2.8 only)**
    Generation 2.8 adds inline tone tags in the text. Syntax: `(tag)` inline in the script.

    Available tags: `(laughs)`, `(chuckle)`, `(sighs)`, `(gasps)`, `(clears throat)`, `(coughs)`, `(sneezes)`

    Unlike `emotion` (which is model-wide), tone tags apply at the point they appear in the text. Combine both:
    ```
    emotion: calm
    text: "I told you this wouldn't work. (sighs) And here we are."
    ```
    Pause syntax still works alongside tone tags: `"(laughs) I can't believe it. <#0.5#> Neither can you."`

    **speech-2.8-turbo.md**
    Frontmatter: same as 2.8-hd. Model: Speech-2.8-Turbo. Gotchas: 2.8-hd gotchas + HD/Turbo latency note.
    Body: identical to 2.8-hd in parameters and tone tags. HD/Turbo trade-off applies as per other generations.
  </action>
  <verify>
    Run: `node scripts/validate-frontmatter.js skills/tools/minimax/speech-02-turbo.md` — PASS.
    Run: `node scripts/validate-frontmatter.js skills/tools/minimax/speech-2.6-hd.md` — PASS.
    Run: `node scripts/validate-frontmatter.js skills/tools/minimax/speech-2.8-hd.md` — PASS.
    Confirm: `grep "(laughs)\|(sighs)\|(gasps)" skills/tools/minimax/speech-2.8-hd.md` returns matches.
    Confirm: `grep "(laughs)\|(sighs)\|(gasps)" skills/tools/minimax/speech-2.8-turbo.md` returns matches.
    Confirm: `grep "Filipino\|Tamil\|Persian" skills/tools/minimax/speech-2.6-hd.md` returns a match.
  </verify>
  <done>All 5 files are status: draft, pass schema validation, and each documents its generation-specific differences. 2.8 files include tone tag syntax and examples. 2.6 files note language additions.</done>
</task>

</tasks>

<verification>
- `node scripts/validate-frontmatter.js skills/tools/minimax/` passes all 7 model files
- speech-02-hd.md is the complete reference with all 4 params documented
- speech-2.8-hd.md and speech-2.8-turbo.md both include the tone tag section
- All files have status: draft (not stub)
</verification>

<success_criteria>
All 6 MiniMax speech model files are at draft status. Claude can read speech-02-hd.md for the complete parameter reference, then read any generation-specific file for its differences. 2.8 files document tone tag syntax with examples. Generation 2.6 files note expanded language support.
</success_criteria>

<output>
After completion, create `.planning/phases/04-voice-skills-content/04-04-SUMMARY.md`
</output>
