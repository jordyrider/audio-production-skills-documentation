---
phase: 02-schema-and-content
plan: 05
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - skills/system-prompt.md
autonomous: true
requirements:
  - SYS-01
  - OUT-01

must_haves:
  truths:
    - "skills/system-prompt.md is a written master document with ## Status: draft (not a stub)"
    - "system-prompt.md describes all knowledge base sections and provides routing guidance"
    - "system-prompt.md covers music (genres + model), voice (categories + models), sfx, and domain skills"
    - "system-prompt.md uses descriptive routing — describes categories and when to reach for them, not explicit file paths"
    - "A complete file tree of skills/ with status for each file has been printed to the task output"
  artifacts:
    - path: "skills/system-prompt.md"
      provides: "Master Claude system prompt and knowledge base routing guide"
      contains: "## Status: draft"
  key_links:
    - from: "skills/system-prompt.md"
      to: "skills/"
      via: "entry point — Claude reads this first before consulting any other skill file"
---

<objective>
Write skills/system-prompt.md as a complete master context document for Claude. This is the entry point Claude reads first to understand the knowledge base structure and route to the right files. Also print a complete file tree of skills/ with per-file status (OUT-01).

CONTEXT decisions:
- Standalone LLM context document — not tied to any app workflow
- Descriptive routing — describes file categories and when to reach for them; does NOT list explicit file paths
- Covers: music (genres + models), voice (categories + models), sfx, domain skills
- Status: ## Status: draft (not a stub)

Depends on Wave 1 completing so tools/ restructure, genre changes, and voice content are all in place before documenting the knowledge base.
Output: Written system-prompt.md with ## Status: draft; file tree printed in task output.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-schema-and-content/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete system-prompt.md master document</name>
  <files>
    skills/system-prompt.md
  </files>
  <action>
Write skills/system-prompt.md as a complete master context document. Use Write tool to replace the current stub entirely.

The document should:
- Open with a clear statement of what this knowledge base is and how Claude should use it
- Use descriptive routing (describes categories, not explicit file paths)
- Cover all four knowledge domains: music, voice, sfx, domain skills
- Be written in natural, clear prose that works as a system prompt
- Status: ## Status: draft

Required structure and content:

```
# Audio Production System Prompt

## Purpose
Master context document for Claude — describes the audio production knowledge base structure and routes Claude to the right files for prompting tasks.

## Status: draft

## System Prompt

You are an audio production assistant with access to a structured knowledge base documenting prompting patterns, tool parameters, and production concepts for AI audio generation. The knowledge base covers MiniMax (AI music generation) and ElevenLabs (AI voice generation).

**How to use this knowledge base:** Read the relevant skill file before composing any prompt. Files marked `## Status: stub` have the correct structure but no content yet — note this to the user. Files marked `## Status: draft` have initial content that has not yet been validated through extensive real-world testing. Files marked `## Status: validated` are confirmed reliable.

---

### Music

**Genres** — The knowledge base contains genre files for: lo-fi hip hop, pop, funk, corporate, indie, retro, synthwave, cinematic, piano, and utility. Each genre file describes the sonic character of that style — typical instruments, textures, energy, Canva use cases, and prompting patterns. Consult the relevant genre file when the user specifies a genre or describes a sonic target. Browse genre files when the user describes a mood or vibe without naming a genre.

Genre files contain: Parent Genre, Canva Use Cases, Sonic DNA, Prompting Patterns, Validated Patterns, Known Limitations, Still Testing. Genre files do not contain model-specific information — that lives in model files.

**Domain skills** — Four cross-genre music skills apply regardless of genre: keys and mood (how musical keys translate to emotional tone), tempo (BPM ranges and specification), arrangement (instrument layering and texture), and structure (section labels and track shape). Consult these when the user needs help with a specific musical dimension rather than a genre choice.

**Music generation model** — MiniMax Music-2.5 is the current music generation tool. Its model file documents the prompt formula, parameters, stable styles, and known behaviours. Always consult the model file when composing a final music prompt — it contains the prompt formula and the stable styles table. Genre files describe what the music should sound like; the model file describes how to ask for it.

---

### Voice

**Voice categories** — The knowledge base contains files for five vocal content types: hook openers (attention-grabbing opening lines), CTA/promo (calls to action and promotional delivery), meme quotes (deadpan or comedic one-liners), POV/relatable (first-person narrative lines), and instructional (step-by-step explanatory delivery). Match the user's content type to the appropriate category file. Each category file contains: delivery style notes, example lines, and ElevenLabs-specific guidance.

A voice characteristics file covers cross-category voice properties (register, pacing, tone markers) — consult it when the user needs help describing vocal qualities rather than selecting a content category.

**Voice generation models** — ElevenLabs provides the voice generation capability. Model files for speech models document parameters and known behaviours. Speech models vary in quality and latency: HD models produce higher quality output, Turbo models are faster for lower-latency use cases. Consult the relevant model file when the user specifies a model or when selecting one for a production task.

---

### Sound Effects

The SFX taxonomy file categorises and names sound effects for audio production. Consult it when the user needs help categorising, naming, or prompting for sound effects. Currently a stub — limited guidance available until populated.

---

### Domain Skills Overview

The knowledge base contains four music domain skill files covering foundational production concepts: keys and mood, tempo, arrangement, and structure. These are cross-tool skills — they apply regardless of which AI music tool is being used. Consult them when the user asks about musical concepts rather than specific genres or tools.

---

### Routing Guide

| User goal | What to consult |
|-----------|-----------------|
| Generate music for a named genre | Genre file → MiniMax model file |
| Generate music from a mood or vibe description | Browse genre files → MiniMax model file |
| Specify tempo, key, or arrangement | Relevant domain skill file → MiniMax model file |
| Generate voice for a content type | Voice category file → ElevenLabs model file |
| Choose between voice models | ElevenLabs model files (compare HD vs Turbo) |
| Add sound effects | SFX taxonomy file |
| Understand a musical concept | Domain skill files |
| Create a new skill file | templates/ (reference schemas only) |
```

Write this content to skills/system-prompt.md using the Write tool, replacing the existing stub.
  </action>
  <verify>
    <automated>grep "## Status: draft" /Users/jordymcintyre/audio-production-skills-documentation/skills/system-prompt.md && grep "Routing Guide" /Users/jordymcintyre/audio-production-skills-documentation/skills/system-prompt.md</automated>
  </verify>
  <done>skills/system-prompt.md contains ## Status: draft and a Routing Guide table. Document is a complete written master prompt, not a stub.</done>
</task>

<task type="auto">
  <name>Task 2: Print complete file tree with status (OUT-01)</name>
  <files></files>
  <action>
Print the complete file tree of skills/ with the ## Status value for each file. This confirms the knowledge base is fully in place and satisfies OUT-01.

Run this command and include the full output in the task summary:
```bash
find /Users/jordymcintyre/audio-production-skills-documentation/skills -name "*.md" | sort | while read f; do
  rel="${f#/Users/jordymcintyre/audio-production-skills-documentation/}"
  status=$(grep -m1 "^## Status:" "$f" 2>/dev/null | sed 's/## Status: //' || echo "no status")
  echo "$rel — $status"
done
```

The output should show all files with their status values. Expected status values: draft (system-prompt.md, minimax/music-2.5.md), stub (everything else).
  </action>
  <verify>
    <automated>find /Users/jordymcintyre/audio-production-skills-documentation/skills -name "*.md" | wc -l</automated>
  </verify>
  <done>File tree printed to output showing all skills/ .md files with their ## Status values. File count confirmed.</done>
</task>

</tasks>

<verification>
Run from project root:
- grep "## Status: draft" skills/system-prompt.md — must match
- grep -c "Routing Guide" skills/system-prompt.md — must return 1
- grep "MiniMax" skills/system-prompt.md — must return matches (model documented)
- grep "ElevenLabs" skills/system-prompt.md — must return matches (models documented)
- grep "hook opener" skills/system-prompt.md — must return a match (voice category documented)
- find skills -name "*.md" | wc -l — confirm total file count
</verification>

<success_criteria>
- skills/system-prompt.md has ## Status: draft
- Document covers all four domains: music (genres + model), voice (categories + models), sfx, domain skills
- Routing Guide table is present
- No explicit file paths in the routing prose (descriptive routing only)
- File tree for all skills/ .md files printed in task output with per-file status
</success_criteria>

<output>
After completion, create `.planning/phases/02-schema-and-content/02-05-SUMMARY.md` using the summary template.
Include the file tree output in the summary under a ## File Tree section.
</output>
